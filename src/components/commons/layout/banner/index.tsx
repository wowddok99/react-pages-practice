import styled from "@emotion/styled"

const BannerWrapper = styled.div`
    height: 300px;
`

export default function LayOutBanner(){
    return (
        <BannerWrapper>
            <div>여기는 배너입니다</div>
        </BannerWrapper>
    )
}