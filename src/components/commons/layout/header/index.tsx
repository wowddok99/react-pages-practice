import styled from "@emotion/styled"

const HeaderWrapper = styled.div`
    height: 100px;
`

export default function LayOutHeader(){
    return (
        <HeaderWrapper>
            <div>여기는 헤더입니다</div>
        </HeaderWrapper>
    )
}